{
    "name": "@rinjani/worker",
    "version": "0.1.0",
    "private": true,
    "type": "module",
    "scripts": {
        "_comment": "Holds the feed-sync implementations (apps/worker/src/feeds/*) and a handful of one-off CLI helpers. There is NO worker process — BullMQ workers + scheduler + work listener all run inside the @rinjani/api process; the BullMQ scheduler is the single source of truth for feed-sync dispatch. The CLI helpers below are for ad-hoc manual runs from a developer shell only.",
        "build": "tsc",
        "sync": "tsx src/sync.ts",
        "sync:feeds": "tsx src/feeds/index.ts all",
        "sync:alienvault": "tsx src/feeds/index.ts alienvault",
        "sync:cisa": "tsx src/feeds/index.ts cisa",
        "sync:abusessl": "tsx src/feeds/index.ts abusessl",
        "feeds:daemon": "tsx src/feeds/index.ts daemon"
    },
    "dependencies": {
        "@rinjani/core": "workspace:*",
        "@rinjani/db": "workspace:*",
        "bullmq": "^5.77.3",
        "dotenv": "^17.2.3",
        "drizzle-orm": "^0.45.2",
        "ioredis": "^5.10.1"
    },
    "devDependencies": {
        "@types/node": "^20.11.0",
        "tsx": "^4.7.0",
        "typescript": "^5.4.0"
    }
}
